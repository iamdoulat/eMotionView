import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function BulkExportPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Bulk Export Products</CardTitle>
        <CardDescription>Export your product data to a CSV file. This feature is under construction.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Coming soon!</p>
      </CardContent>
    </Card>
  );
}
