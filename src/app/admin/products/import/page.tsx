import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function BulkImportPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Bulk Import Products</CardTitle>
        <CardDescription>Import products from a CSV file. This feature is under construction.</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Coming soon!</p>
      </CardContent>
    </Card>
  );
}
